# Terraform Project

This project allow me to provide VMs to my cluster proxmox.

Reusable modules ares created in `modules` folder.

# TODOs / Roadmap :

 - [ ] Add HashiCorp Vault to get auth token (actually I export the variable in terminal)
 - [ ] Generate signed certificates to secure the connexion
 - [ ] Correctly handle provider configuration (to avoid changing it when i'm outside)
