storage "file" {
  path = "{{ vault_directory }}/data"
}

listener "tcp" {
  address = "0.0.0.0:8200"
  tls_disable = "{{ vault_tls_disabled }}"
}

ui = "{{ vault_ui }}"
disable_mlock = true
log_level = "Debug"
